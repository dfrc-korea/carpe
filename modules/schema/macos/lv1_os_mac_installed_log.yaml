Name: INSTALLED_LOG
Desc: Mac Operating Installed Log
Table:
  - TableName: lv1_os_mac_installed_log
    Columns:
      - par_id
      - case_id
      - evd_id
      - data

    Types:
      - [VARCHAR(255), NOT NULL]
      - [VARCHAR(255), NOT NULL]
      - [VARCHAR(255), NOT NULL]
      - [TEXT, NOT NULL]

Artifacts:
  - Name: INSTALLED_LOG
    Desc: Mac Operating Installed Log
    Values:
      - [source, REG]
