Name: MACADDRESS
Desc: Windows MAC ADDRESS
Table:
  - TableName: lv1_os_win_reg_mac_address
    Columns:
      - par_id
      - case_id
      - evd_id
      - mac_address
      - description
      - backup_flag
      - source_location

    Types:
      - [VARCHAR(255), NOT NULL]
      - [VARCHAR(255), NOT NULL]
      - [VARCHAR(255), NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT, NOT NULL]

Artifacts:
  - Name: MACADDRESS
    Desc: Windows MAC ADDRESS
    Values:
      - [source, REG]