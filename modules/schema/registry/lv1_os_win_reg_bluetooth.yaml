Name: Bluetooth
Desc: bluetooth
Table:
  - TableName: lv1_os_win_reg_bluetooth
    Columns:
      - par_id
      - case_id
      - evd_id
      - name
      - key_last_updated_time
      - lastconnectedtime
      - backup_flag
      - source

    Types:
      - [VARCHAR(255), NOT NULL]
      - [VARCHAR(255), NOT NULL]
      - [VARCHAR(255), NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT]
      - [TEXT, NOT NULL]

Artifacts:
  - Name: Bluetooth
    Desc: bluetooth
    Values:
      - [source, REG]