Name: NETWORKDRIVE
Desc: Windows NETWORK DRIVE
Table:
  - TableName: lv1_os_win_reg_network_drive
    Columns:
      - par_id
      - case_id
      - evd_id
      - network_drive_name
      - modified_time
      - ordering
      - useraccount
      - backup_flag
      - source_location

    Types:
      - [VARCHAR(255), NOT NULL]
      - [VARCHAR(255), NOT NULL]
      - [VARCHAR(255), NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT, NOT NULL]
      - [TEXT, NOT NULL]

Artifacts:
  - Name: NETWORKDRIVE
    Desc: Windows NETWORK DRIVE
    Values:
      - [source, REG]